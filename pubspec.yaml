name: gg_repo_manager
description: Repository manages all of our repos, i.e. updating, maintaining, establishing coding rules, configuring projects.
version: 1.0.0
repository: https://github.com/inlavigo/gg_repo_manager

environment:
  sdk: ^3.2.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  args: ^2.4.2
  coverage: ^1.7.2
  lints: ^3.0.0
  pana: ^0.21.45
  test: ^1.24.0
  yaml: ^3.1.2
